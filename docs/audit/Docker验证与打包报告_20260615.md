# Docker 全面验证 + 桌面打包报告（2026-06-15）

> 在 P0 数据正名修复 + peer-review 问题整改后，对系统做 Docker 容器全面验证，并完成 macOS .app/.dmg 桌面打包。
> 遵循 RIPER-5；甲方需求闭环 > 数据真实性 > 可验收交付。

---

## 一、Git 提交记录（4 个提交）

| 提交 | 类型 | 内容 |
|------|------|------|
| `19d3937` | fix(P0) | 数据真实性正名 — 模拟特征表去除"真实"冒名标注 + ID 泄漏剔除 + is_real_data 标记 |
| `fb70587` | feat | EDA 科研级可视化/三层离线地图/批量导入/附件下载/报告图件/打包自检 |
| `90cdada` | chore | 同步其余代理改动（config/main/FieldMappingPage/mappings） |
| `acc5831` | fix(packaging) | srs.spec 改 onedir 模式，修复 .app 稳定性（peer-review Major） |

**.gitignore 调整**：排除 raw 大数据文件（模拟特征表 25MB / merged_std33 5MB 本地保留），data/geo（27MB 离线地图必需）入库。

---

## 二、Docker 全面验证（镜像 `srs-backend:verify-final`）

容器内含本轮全部新代码 + Noto CJK 中文字体 + matplotlib。

| # | 验证项 | 命令 | 结果 |
|---|--------|------|------|
| ① | 全量 pytest | `pytest -q` | ✅ **71 passed, 2 skipped, 10 warnings**（63s） |
| ② | 极端 10 场地闭环 | `run_system_extreme_validation.py` | ✅ n=10, accuracy=1.0, quality=100, **8 闭环全 true** |
| ③ | 数据闭环 | `build_dataset_splits.py` | ✅ 8 splits，synthetic 标记正确，real/合成隔离 |
| ④ | RAG 检索 | `retrieve("砷修复")` | ✅ 命中 1 因子/8 阈值/8 技术（带来源） |
| ④ | RAG 同义词 | `retrieve("Pb超标")` | ✅ Pb→铅 精确命中 |
| ⑤ | GLM 在线 | `chat("铅污染修复")` | ✅ configured=True, degraded=None，结构化专业输出 |
| ⑥ | 报告图件 | `report_service.collect` | ✅ map_image 非空（80KB base64）+ Noto CJK 中文字体正常 |

**闭环明细**（②）：import / diagnosis / evaluation / recommendation / report / workflow_traceability / file_upload_download / ai_rag —— **全部 true**。

---

## 三、桌面打包（macOS .app + .dmg）

### 打包配置
- **模式**：onedir（修复 peer-review Major ".app onefile 不稳"）
- **工具**：PyInstaller 6.20 + hdiutil
- **包含资源**：data/geo（27MB 离线行政区底图）+ frontend/dist（2.5MB）+ matplotlib/sklearn/weasyprint/Noto CJK 字体 + reporting 模板 + knowledge_base

### 产物
| 产物 | 路径 | 体积 | 说明 |
|------|------|------|------|
| SRS.app | `backend/dist/SRS.app` | 388MB | onedir .app bundle，含完整 Contents |
| SRS-0.1.0.dmg | `backend/dist/SRS-0.1.0.dmg` | 174MB | UDZO 压缩 DMG，含 Applications 快捷方式 |

### .app 验证
- ✅ 可执行文件：`arm64` Mach-O（原生 Apple Silicon）
- ✅ frozen 环境导入：argparse/后端模块/数据资源全部正常
- ✅ 启动首启自检（`run_preflight`）：
  ```
  ✅ [端口检测] 端口空闲可用
  ✅ [数据库] SQLite 目录可写
  ✅ [Redis] 可达
  ⚠️ [AI 大模型] 未配置 key（降级知识库检索，不阻断）
  ⚠️ [地图底图] 未配置地图服务 key（L1 离线底图可用，不阻断）
  结果: 0 项阻断, 2 项警告, 3 项正常 ✅ 可正常启动
  ```
- ✅ 后端 health 检查：`{"status":"ok"}`
- ✅ 登录接口：200 OK

### DMG 验证
- ✅ checksum VALID（hdiutil verify 通过）
- ✅ 内容：SRS.app + Applications 快捷方式（标准 macOS 分发格式）
- ✅ 挂载/卸载正常

---

## 四、peer-review 问题整改对照

| peer-review 问题 | 整改 | 验证 |
|------------------|------|------|
| 🚨 Major 1：数据冒充真实 | P0 正名（19d3937）| ✅ is_real_data:False + 文件/版本/文档同步 |
| 🚨 Major 2：AUC=1.0 ID 泄漏 | 剔除 ID + 如实标注 | ✅ dropped_leakage_cols 记录 + AUC 虚高警告 |
| Major 3：异常吞没 | （P1 待办，本轮未改）| ⚠️ 11 处 except 待加日志 |
| Major 4：前端无 ErrorBoundary | （P1 待办，本轮未改）| ⚠️ 待加 |
| Major（打包）：onefile .app 不稳 | onedir 模式（acc5831）| ✅ .app 启动自检通过 |
| Minor：Docker 中文字体 | Noto CJK 装入 Dockerfile | ✅ Glyph 警告 52→0 |

---

## 五、交付物清单

### 代码提交（git）
- 4 个提交，工作区干净，无敏感文件入库。

### 可执行交付
- `backend/dist/SRS-0.1.0.dmg`（174MB）— macOS 安装镜像，可直接分发给甲方。
- `backend/dist/SRS.app`（388MB）— 解压即用的 .app bundle。

### 文档
- `data/raw/README.md` — 数据血缘说明
- `docs/algorithms/rf_shap.md` / `ml/artifacts/MODEL_README.md` — 算法与模型如实标注
- `docs/audit/peer_review_深度审查报告_20260615.md` — 同行评审报告
- `docs/audit/甲方需求对照表_20260615.md` — 需求逐条对照
- `docs/地图离线方案与地图服务配置.md` — 地图方案
- 本报告 — Docker + 打包验证

---

## 六、残留风险（如实标注）

1. **模型 AUC=1.0 虚高**：当前模型基于模拟特征表（is_real_data:False），剔除 ID 后 AUC 仍 1.0，证实标签由特征确定性派生。**已如实标注，不可外推真实场地**。真实数据模型（merged_std33）待重建，预期 AUC 0.7-0.9。
2. **peer-review Major 3/4**：异常吞没改日志、前端 ErrorBoundary，列为 P1 后续迭代。
3. **DMG 未签名公证**：当前 DMG 未做 Developer ID 签名 + 公证，甲方首次打开需右键"打开"绕过 Gatekeeper。正式分发需 Apple 开发者账号签名公证。
4. **地图服务 L2 在线**：仅服务器固定 IP 场景；桌面打包用 L1 离线底图（已内置）。
5. **乡镇级地图**：无免费开放数据源，县级为离线极限（匹配业务粒度）。

---

## 七、结论

- **Docker 全面验证**：6 项（pytest/极端/数据闭环/RAG/GLM/报告）全部通过。
- **桌面打包**：onedir .app（388MB）+ DMG（174MB arm64）成功，首启自检正常，可分发。
- **P0 红线已清除**：数据真实性正名完成，交付的是"诚实标注局限、经得起审查的真实工程"。
- **peer-review Major 已整改 3/4**（数据/ID/打包），剩 2 项（异常日志/ErrorBoundary）为 P1 迭代。

**系统级验收通过，DMG 可交付甲方评审。**
