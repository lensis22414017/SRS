# 给 CC 的 SRS 深水区整改提示词（2026-06-25）

下面内容可直接发给 CC/Claude Code 执行。

---

ENTER EXECUTE MODE

你现在继续修复 `/Users/lensis/Claude/Projects/SRS` 的 SRS 系统。请先阅读并以此为硬验收基线：

- `/Users/lensis/Claude/Projects/SRS/docs/audit/browser_acceptance_20260624/final_2352/FINAL_REPORT.md`
- 截图证据目录：`/Users/lensis/Claude/Projects/SRS/docs/audit/browser_acceptance_20260624/final_2352/screenshots`
- 最新 PDF/DOCX 证据目录：`/Users/lensis/Claude/Projects/SRS/docs/audit/browser_acceptance_20260624/final_2352/downloads`

总目标：不是“看起来能跑”，而是让数据导入、地图、EDA、AI/RAG、追溯报告、首页统计和多场地管理真实闭环，且后端测试、前端构建、浏览器验收全部可复核。

## 一、最高优先级：先修硬失败

### 1. 后端测试必须全绿

当前失败：

- `tests/test_data_pipeline.py::test_api_batch_import_and_overview_badges`
- `tests/test_workflow_report.py::test_report_html_renders`

要求：

- `cd backend && .venv/bin/pytest -q` 必须通过。
- 报告测试不要再强制要求“操作日志摘要”，因为甲方明确要求报告中可以不提系统操作摘要。
- 新测试应检查：地图图件、采样点空间分布、SHAP/EDA 图、功能重构、SSUI、附件清单、版本信息。

### 2. AI/RAG 真实连通性必须修正

当前问题：

- `/api/v1/ai/status` 显示 `configured=true`。
- `/api/v1/system/ai-config/test` 实际返回 `HTTP 401 Unauthorized`。
- AI 聊天只是 RAG 降级，不是真 LLM 成功调用。

要求：

- 状态必须拆成：`has_config`、`connectivity_ok`、`last_test_error`、`provider`、`model`。
- 配置了 key 不等于连通成功，UI 不得误导。
- OpenAI base_url 不应配 `Qwen3.5-9B-MLX-4bit` 这类不匹配模型。
- 至少验证一个可用 provider；如果本机没有可用 key，要给明确阻塞提示和配置入口，不得假装成功。

### 3. OP 有机场地不能再出现裸 `null/NaN`

当前问题：

- `site_北京_OP_200点` 的 SSUI、功能重构、推荐仍然是 `null/无足够指标/404`。

要求：

- 对有机场地建立可解释降级路径。
- 如果 SSUI/功能重构缺少元指标，页面要显示“为什么不能算、缺哪些指标、还能给出哪些 OP 风险诊断”。
- 不允许仪表盘显示 `NaN`、分数显示 `null 分`。
- 推荐方案至少能基于 OP 因子给出有机污染修复技术候选；若数据源无超标/无阈值，也要明确原因。

## 二、全流程追溯：文件上传必须真实可用

甲方新增反馈：全流程追溯中，上传文件功能还是实现不了。你必须按真实业务路径重测和修复。

### 4. 五阶段上传/下载/刷新持久化

阶段：

- 调查评估
- 方案审批
- 施工监理
- 效果评估
- 后期管护

要求：

- 每个阶段都能上传文件。
- 支持 PDF、DOCX、XLSX、PNG/JPG、Markdown/文本。
- 支持中文文件名。
- 上传后刷新页面仍存在。
- 下载文件内容与原文件一致。
- 文件记录必须绑定：`site_id`、`stage`、`file_role`、`operator_id`、`timestamp`、`file_object_id`。
- 不允许跨场地、跨阶段下载到别的文件。

### 5. 监理上传 -> 甲方审批 -> 盖章后再上传

要求：

- 追溯阶段至少支持文件角色：原始报告、审批意见、盖章版报告、补充材料。
- 页面能看出文件属于哪个阶段、哪个角色、谁上传、何时上传。
- 如果暂不做电子签章，也必须支持“盖章版文件上传”和状态流转。

验收证据：

- 提交 5 个阶段各自上传后的截图。
- 提交刷新后仍存在的截图。
- 提交至少 1 个下载文件的校验记录（大小/hash 或文本内容一致）。

## 三、地图：场地详情不能混入全国地图数据

甲方新增反馈：点击具体场地详情后，弹出来的是全国采样点数据。场地详情地图不能和全部场地地图混为一谈。

### 6. 拆清首页地图与场地详情地图的数据作用域

要求：

- 首页“数据概览”地图：只展示全部场地分布，点位粒度是场地。
- 场地详情地图：只展示当前 `site_id` 的采样点、当前场地采样区域/外包边界、当前场地污染物分级。
- 场地详情页不能请求或渲染全国全部采样点。
- 后端 map/layers 必须按 `site_id` 过滤，并在测试中断言所有点属于当前场地。

验收证据：

- 首页全国地图截图。
- 场地 1 详情地图截图，标注点位数。
- 场地 2 详情地图截图，标注点位数。
- 浏览器或接口日志说明三个地图的数据量不同且作用域正确。

## 四、EDA：云雨图不是直方图，中文必须可读

甲方新增反馈：云雨图和直方图区分错误；EDA 中说是云雨图实际上是直方图；有些文字仍显示不出。

### 7. 正确定义并实现 EDA 图表类型

要求：

- 直方图：显示分箱频数/密度，标题与 tab 必须叫直方图。
- 云雨图/raincloud plot：必须包含分布云（density/half violin）、雨点（jitter points）和箱线/摘要中的至少两类视觉元素。
- 如果暂不能实现真正云雨图，就不要把直方图命名为云雨图。
- 分组对比不能空白；若无区域字段，应自动降级到深度、污染等级、采样点簇或超标等级，并说明原因。

### 8. 中文字体和图表文字

要求：

- ECharts/Canvas/SVG/PDF/DOCX 图中中文标题、坐标轴、图例、tooltip、频次/密度等文字必须显示。
- 不得出现方块、乱码、缺字、遮挡。
- 直方图中的“频次”等轴标题必须可见。

参考目录：

- `/Users/lensis/Desktop/Python可视化代码精品案例合集`
- `/Users/lensis/Desktop/Python地理空间数据可视化`
- `/Users/lensis/Desktop/Python可视化代码专题分享`
- `/Users/lensis/Claude/Projects/SRS/000/10.SHAP机器学习分析.ipynb`

验收证据：

- 每个 EDA tab 截图。
- 单独截图证明“直方图”和“云雨图”不是同一种图。
- 说明不可用图表的降级原因。

## 五、首页数据概览：颜色、标签、真实性

### 9. 污染类型颜色必须全系统一致

甲方新增要求：首页污染类型分布图三类颜色不要同色系，要和场地详情一致。

颜色语义：

- 重金属：红色
- 有机：紫色
- 复合污染：橙色

要求：

- 首页污染类型分布、场地标签、地图点位、详情页标签尽量使用同一套语义色。
- 不允许首页一套颜色、详情页另一套颜色。
- 图例文字必须清楚。

### 10. 各场地超标记录排行标签要短而可读

甲方新增要求：各场地超标记录图中，只显示“北京场地”“新疆场地”这类短名称即可；如果同省有多个场地，用“北京1”“北京2”或“北京场地1”“北京场地2”区分。

要求：

- 不显示长文件名，如 `site_浙江_HM+OP_15点` 这种可以在 tooltip 中显示，轴标签不要显示。
- 单省单场地：显示“北京场地”“新疆场地”等。
- 同省多个场地：显示“北京1”“北京2”或“北京场地1”“北京场地2”。
- tooltip 中保留完整场地名、site_id、污染类型、超标数。
- 横轴文字不得截断、重叠、不可读。

### 11. 首页统计必须真实，不得虚假

要求：

- 场地总数、覆盖省份、采样点总数、超标记录数来自真实后端统计。
- 重复导入的数据不得污染首页统计；至少默认按“有效场地/最新版本”统计。
- 如果展示的是测试库，应在开发验收记录中说明。

## 六、批量导入：加去重和版本策略

当前问题：同一文件多次导入会重复造场地，导致首页统计污染。

要求：

- 以 `site_code`、文件内容 hash、场地名+省份+采样点坐标摘要建立幂等判断。
- 导入冲突时 UI 提供：跳过、覆盖、作为新版本导入。
- 作为新版本导入时，首页默认只统计最新有效版本。
- 导入结果中显示是否新增、跳过、覆盖、创建新版本。

## 七、报告：PDF 和 DOCX 必须一致

当前问题：

- PDF v10 已包含地图和 SHAP 图。
- DOCX v11 只有 1 张媒体图，不包含 SHAP，也没有数据分析图。

要求：

- DOCX 至少同步嵌入地图图件、SHAP 排名图、1-5 张 EDA/分析图。
- PDF/DOCX 章节口径一致。
- 报告中不出现“系统操作摘要/操作日志摘要”。
- 报告中的图必须对应真实场地数据，不得用占位图。

验收证据：

- `pdfimages -list` 结果。
- DOCX 解包后的 `word/media/*` 数量。
- `pdftotext` 和 DOCX XML 文本 grep：地图、采样点、SHAP、功能重构、SSUI、附件清单。

## 八、UI 风格：政务监管软件，不要 AI 味

要求：

- 整体风格要像政府机关/监管平台：克制、稳定、清楚、功能丰富、易操作。
- 不要大面积渐变、花哨 AI 风格、营销式卡片堆砌。
- 表格、筛选、导出、下载、详情、审批操作要清晰。
- 图表可以好看，但必须先真实可读。

## 九、必须交付的验证清单

完成整改后必须提供：

1. 已改文件清单。
2. 后端测试：`cd backend && .venv/bin/pytest -q`。
3. 前端构建：`cd frontend && npm run build`。
4. 浏览器截图目录，至少覆盖：
   - 首页数据概览
   - 首页地图
   - 场地详情地图（至少 2 个场地）
   - 批量导入冲突/去重
   - EDA 直方图
   - EDA 云雨图
   - EDA 分组对比
   - AI 配置测试
   - AI/RAG 对话
   - 五阶段追溯上传
   - 追溯刷新后文件仍存在
   - PDF/DOCX 报告列表和下载
5. 报告文件验证：
   - 最新 PDF 下载路径
   - 最新 DOCX 下载路径
   - PDF 图片数量
   - DOCX 图片数量
6. 明确剩余风险；不得把未修好的内容写成已完成。

## 十、不得做的事

- 不得伪造数据、标准、文献、模型性能。
- 不得改原始检测值。
- 不得硬编码 key。
- 不得只修 UI 假象，后端接口仍返回 null/404。
- 不得把直方图叫云雨图。
- 不得把场地详情地图和首页全国地图混用。
- 不得让测试失败后仍称“完成”。

最终交付门槛：`pytest` 全绿、前端构建通过、浏览器关键流程截图齐全、AI 状态真实、OP 场地有合理降级闭环、PDF/DOCX 报告一致、首页/场地详情地图作用域正确。
