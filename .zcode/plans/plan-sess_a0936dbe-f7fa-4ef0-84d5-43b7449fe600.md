# 软件著作权三件套 · P0+P1 深度修复计划（应对 AI Agent 审计）

## 决策依据
- 软件全称：以名称A 为准（甲方要求+一直使用），即"污染场地土壤生态-生产功能重构监管系统"
- 版本号：源码回退到 1.0.1（裴总决策），文档中 1.0.2→1.0.1 文本替换
- 官方硬性要求：A4、单面黑白、页眉一致、页码右上角、不得涂改、前后30页连续

## 步骤 0：备份
- 备份当前三份 DOCX 到 `_backup_原版_20260722/v4_pre_fix/`

## 步骤 1：说明书 P0+P1 修复
新建 `fix_manual_v4.py`：
- **P0-1 封面名称统一**：封面两行改为"污染场地土壤生态-生产功能重构监管系统"（与申请表一致）
- **P1-2 元数据清洗**：author→编制单位简称、last_modified_by→空、created/modified→2026-07-22、title→软件全称
- **P1-4 添加 21 张表格的表注**：在每张表格前插入"表 X-Y 表名"段落（按章节编号：表1-1、表2-1...表8-5），居中、黑体10pt

## 步骤 2：源程序文档 P0+P1 修复
新建 `fix_source_v4.py`：
- **P0-1 封面名称统一**：同说明书
- **P0-2 版本号回退**：遍历所有代码段落，文本替换 `1.0.2`→`1.0.1`、`v1.0.2`→`v1.0.1`、`V1.0.2`→`V1.0.1`（共约 14 处）
- **P1-1 行数统一**：封面与结尾声明改为"约 34,143 行"（与申请表一致，口径含 CSS/JSX）
- **P1-2 元数据清洗**：同说明书
- **P1-5 密钥占位符中性化**：`CHANGE_ME_IN_ENV`→`REPLACE_WITH_ENV_SECRET`、`Demo@2026`→`REPLACE_WITH_ENV_PASSWORD`

## 步骤 3：申请表 P0+P1 修复
新建 `fix_apply_v4.py`：
- **P0-3 占位符提示强化**：在著作权人信息表的 3 个占位符字段加红色醒目标记"⚠ 此处需填写真实信息后提交"
- **P1-3 日期字段**：首次发表日期勾选"未发表"；申请日期留空（提交时填）
- **P1-2 元数据清洗**：同说明书
- **P1-6 软件简称**：去除全角括号，改为"污染场地重构监管系统"（或保留，因官方未明文禁止）

## 步骤 4：跨文件一致性最终校验
新建 `verify_consistency.py`，逐项核查：
1. 三份文件封面软件全称是否 = "污染场地土壤生态-生产功能重构监管系统"
2. 三份文件版本号是否 = V1.0.1（源码内无 1.0.2 残留）
3. 三份文件编制单位是否一字不差
4. 三份文件页眉是否风格一致
5. 三份文件元数据是否已清洗（无 python-docx/曾鸿）
6. 申请表占位符是否已标注
7. 说明书 21 张表格是否都有表注
8. 源程序无 GPT 痕迹、无 1.0.2 残留

## 关键技术约束
1. 版本号替换只在 DOCX 文档内做文本替换，**不修改 SRS-round10 项目源码**
2. 元数据清洗用 python-docx 的 core_properties API
3. 表注插入用 `paragraph.insert_paragraph_before()` 在表格前插入
4. 所有修改保留原格式（字体、字号、对齐）

## 不在范围内
- ❌ 不修改 SRS-round10 项目源码（只改 DOCX 文档内容）
- ❌ 不导出 PDF
- ❌ 不替换为官方在线生成版申请表（需裴总手动操作）
- ❌ 不填写真实证件号码/地址（裴总自行填写）