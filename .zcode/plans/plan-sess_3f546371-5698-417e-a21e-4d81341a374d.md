# 8个问题全量修复计划

## 数据基础：重新导入三份真实场地数据（问题2/5b/7的共同根因）

1. **删除个旧场地(site_id=1)的2个假采样点和旧数据**，保留 site 记录本身
2. **用系统自带导入功能重导三份真实数据**：
   - 个旧HM：`data/raw/3.20250731_重金属污染场地数据表(云南个旧)_最终版.xlsx`（134点）
   - 栖霞OP：`data/raw/2.20250731_有机污染场地数据表(南京栖霞)_完整版.xlsx`
   - 农村HM+OP：`data/raw/1.20250731_复合污染场地数据表(乡村建设用地)_完整版.xlsx`
3. 用 `backend/app/services/import_service.py` 的 `parse()` + `ingest_service.py` 的 `ingest()` 链路，先生成 mapping 再导入
   → 验证：每份文件导入后 `sampling_points` 表行数与原始Excel一致（个旧134点）、经纬度正确（103.14°E）

## 问题3：清除KOS诊断UI中的F1/AUC残留

4. **ObstacleAnalysis.tsx**：
   - 删除/隐藏 `AUC_GUIDE`/`F1_GUIDE` 常量、`aucVal`/`f1Val` 计算、低置信度Alert中的AUC数值、技术详情折叠面板的AUC/F1展示
   - 技术详情改为显示 KOS 模型的 Spearman 指标（从 `model.metrics.cv_spearman_mean` 取）
5. **methodFlows.ts** `obstacle_analysis` 配置：清除"LLM报告润色"desc中的AUC/SHAP、"模型可信度指标"desc中的AUC/F1，改为KOS方法描述
6. **diagnosis_service.py:498** calc_trace 第③条：清除 `AUC=...,F1=...` 文本，改为 `Spearman=...`
7. **SystemManagement.tsx:845,864**：算法描述中 CV AUC 改为 Spearman

## 问题4：清洗AI回答中的多余标点

8. **ai_service.py DIAGNOSIS_POLISH_PROMPT**：添加明确指令"禁止使用markdown加粗(**)、中文引号("")、无意义标点；用纯文本自然段表达"
9. **ai_service.py SYSTEM_PROMPT**（对话用）：同样添加"回答用纯文本，不用markdown加粗或装饰性引号"
10. **ai_service.py 新增 `_clean_markdown_punct()` 后处理函数**：剥离残留的 `**`、`""`转`()`、多余连续标点。在 polish_diagnosis 和 chat 返回前调用
    → 验证：重新触发诊断，AI润色文本无 `**` 和 `""`

## 问题6：文字显示不完整

11. **DashboardScreen.module.css**：`alertName` 和 `traceSite` 的 `white-space: nowrap` 改为 `normal`，允许换行显示完整文字；移除 `text-overflow: ellipsis`
12. 检查 ObstacleAnalysis.tsx 中诊断摘要的 CSS，确保长文本能完整显示（如有 max-height 限制则调整）

## 演示验证（问题1/5a/5b/7）

13. **问题1 角色审批演示**：
    - 用 enterprise 登录截图（菜单只有7项，无系统管理）
    - admin 在系统管理-账户审核 Tab 演示审批流程
    - TraceDetail 演示五阶段流程推进
14. **问题5a EDA演示**：进入场地详情页的"数据分析(EDA)"Tab截图
15. **问题5b 双轨诊断**：对重导的个旧134点场地分别运行生产轨和生态轨诊断，截图对比
16. **问题7 地图验证**：重导后查看Dashboard地图，134个采样点应按真实坐标(103.14°E,23.34°N)分布

## 最终提交推送

17. 运行全量 pytest 确保无回归
18. 生成截图审查报告
19. commit + push 到 GitHub

## 文件变更清单（预估）

| 文件 | 变更类型 |
|---|---|
| `backend/app/services/ai_service.py` | 修改 prompt + 新增 _clean_markdown_punct |
| `backend/app/services/diagnosis_service.py` | 清除 calc_trace 中 AUC/F1 |
| `frontend/src/pages/ObstacleAnalysis.tsx` | 清除 AUC/F1 展示，改用 Spearman |
| `frontend/src/config/methodFlows.ts` | 清除方法说明中 AUC/F1 |
| `frontend/src/pages/SystemManagement.tsx` | 算法描述改 Spearman |
| `frontend/src/pages/DashboardScreen.module.css` | 文字换行修复 |
| 数据库 srs_dev.db | 重导三份场地数据（不入 git） |
