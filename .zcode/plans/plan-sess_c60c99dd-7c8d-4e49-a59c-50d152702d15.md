# SRS Round10 最终交付前全面检查与打包计划

## 现状确认
- 源代码干净：无"裴总/辛特助/甲方/AI助手"等敏感词
- 前端 TypeScript 编译零错误
- 后端 Python 语法检查通过
- SSUI 全量测试：20/30 OK，10 有机降级，0 ERROR

## 执行步骤

### 1. 运行全部测试确认零失败
- `pytest backend/tests/ -x --tb=short` (跳过需要真实数据的 skip 测试)

### 2. 前端生产构建
- `cd frontend && npm run build` 生成 dist/

### 3. Git 提交并推送
- `git add -A && git commit -m "..." && git push`

### 4. PyInstaller 打包
- 使用项目现有的 packaging/srs_setup.iss 或 spec 文件

### 5. 在 exe 中验证
- 启动打包后的 exe，确认页面可访问、SSUI 可运行

### 6. 系统截图
- 使用 Playwright 自动截图：大屏、SSUI结果、诊断、重构、追溯

### 7. 整理交付物
- 创建 `交付物/` 目录，放入 exe、数据文件、截图、文档

### 8. 生成 DOCX 文档
- 使用 python-docx 生成标准公文格式的用户手册
